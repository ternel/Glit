{% extends '::base.html.twig' %}

{% block content %}

<div class="alert alert-warning">
    {% trans %}You should push your repository initialization before being able to browse code, commits, etc. Please
    follow intruction below.{% endtrans %}
</div>

<header>
    <h1>{{ project.name }}</h1>

    <p class="lead">{{ project.description }}</p>

    <div class="subnav">
        <ul class="nav nav-pills">
            <li><a href="#global">Code</a></li>
            <li><a href="#gridSystem">Issues</a></li>
            <li><a href="#fluidGridSystem">Wiki</a></li>
            <li><a href="#gridCustomization">Documents</a></li>
            <li><a href="#layouts">Pull Request</a></li>
            <li><a href="#layouts">Equipe</a></li>
        </ul>
    </div>
</header>

<div class="row-fluid">
    <h2>New Repository</h2>
<pre class="prettyprint">
mkdir {{ project.path }}
cd {{ project.path }}
git init
touch README
git add README
git commit -m "initial commit"
git remote add origin {{ ssh }}
git push -u origin master</pre>

    <h2>Existing Repository</h2>
<pre class="prettyprint">
cd existing_git_repo
git remote add origin {{ ssh }}
git push -u origin master</pre>


    <h2>New Repository</h2>
<pre class="prettyprint">
mkdir {{ project.path }}
cd {{ project.path }}
git init
touch README
git add README
git commit -m "initial commit"
git remote add origin {{ ssh }}
git push -u origin master</pre>
    <h2>New Repository</h2>
<pre class="prettyprint">
mkdir {{ project.path }}
cd {{ project.path }}
git init
touch README
git add README
git commit -m "initial commit"
git remote add origin {{ ssh }}
git push -u origin master</pre>
    <h2>New Repository</h2>
<pre class="prettyprint">
mkdir {{ project.path }}
cd {{ project.path }}
git init
touch README
git add README
git commit -m "initial commit"
git remote add origin {{ ssh }}
git push -u origin master</pre>
    <h2>New Repository</h2>
<pre class="prettyprint">
mkdir {{ project.path }}
cd {{ project.path }}
git init
touch README
git add README
git commit -m "initial commit"
git remote add origin {{ ssh }}
git push -u origin master</pre>


</div>

        {% endblock %}